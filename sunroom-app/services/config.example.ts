export const SUPABASE_URL = "https://your-project-ref.supabase.co";
export const SUPABASE_ANON_KEY = "your-anon-key-here";
export const API_KEY = "your-api-key-here";

// Capture: offer the OPTIONAL 6th point (top of the near corner post) after the
// 5 required ones. Only useful when the structure already exists in the photo —
// LORA captures and re-plots of built jobs — where it stops the drawn corner
// sitting low. On a new-build quote there's no post to click and a guessed one
// drags the whole solve toward the guess, so this ships off.
// See sunroom-backend/sunroom-3d/test_corner_top.js for the measured numbers.
export const CAPTURE_CORNER_TOP_POINT = false;
